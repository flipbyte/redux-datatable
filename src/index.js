import createTable, {
    createReducer,
    reducer,
    requestData,
    receiveData,
    fetchData,
    REQUEST_DATA,
    RECEIVE_DATA,
    REQUEST_DATA_CANCEL
} from './createTable';

import Table from './Table';

export {
    Table,
    createReducer,
    reducer,
    requestData,
    receiveData,
    fetchData,
    REQUEST_DATA,
    RECEIVE_DATA,
    REQUEST_DATA_CANCEL
}

export default createTable;
